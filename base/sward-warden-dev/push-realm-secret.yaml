---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: push-realm-sward-warden
  namespace: auth
spec:
  refreshInterval: 1m0s
  updatePolicy: Replace
  deletionPolicy: Delete
  secretStoreRefs:
    - name: push-secrets-to-sward-warden-dev-ns
      kind: SecretStore
  selector:
    secret:
      name: keycloak-sw-dev-sa
  data:
    - match:
        secretKey: username
        remoteRef:
          remoteKey: keycloak-sw-dev-sa
          property: username
    - match:
        secretKey: password
        remoteRef:
          remoteKey: keycloak-sw-dev-sa
          property: password
