{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "sourceSecretName": {
      "type": "string",
      "description": "The name of the source secret in the cluster"
    },
    "sourceNamespace": {
      "type": "string",
      "description": "The namespace where the source secret resides (e.g., dbs)"
    },
    "remoteNamespace": {
      "type": "string",
      "description": "The target namespace where the secret should be pushed"
    }
  },
  "required": ["sourceSecretName", "sourceNamespace", "remoteNamespace"]
}
